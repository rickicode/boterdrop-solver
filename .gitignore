# Runtime artifacts / local env — never commit
venv/
.venv/
__pycache__/
*.pyc
*.pyo
debug_logs/
*.log
.env
.env.*

# Editor / OS
.idea/
.vscode/
.DS_Store

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# env template IS committed (no secrets)
!.env.example
